name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ruby: ["3.3", "4.0"]
        rails: ["7.1", "8.0"]
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: maglev_test
        ports:
          - "5432:5432"
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/maglev_test
      MAGLEV_REQUIRE_POSTGRESQL: "true"
      BUNDLE_GEMFILE: ${{ github.workspace }}/gemfiles/rails_${{ matrix.rails }}.gemfile
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler-cache: true
      - name: Load test database schema
        working-directory: spec/dummy
        env:
          RAILS_ENV: test
        run: bundle exec rails db:schema:load
      - run: bundle exec rspec
      - run: bundle exec standardrb
      - run: bundle exec rubocop
      - run: bundle exec rake build
      - name: Boot dummy application
        working-directory: spec/dummy
        env:
          RAILS_ENV: test
        run: bundle exec rails runner 'abort unless Rails.application && Maglev::VERSION'
      - run: bundle exec rake maglev:release_audit
